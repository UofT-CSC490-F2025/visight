dataset_version: "augmented"
model_size: "yolov8s.pt"       # base checkpoint
epochs: 3
img_size: 640
batch: 24
workers: 16
seed: 117
notes: "Initial train attempt: No hyperparameter tuning, small YOLO model"
warmup_epochs: 0
dropout: 0.3
n_layers_freeze: 1